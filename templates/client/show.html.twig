{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    
    <div class="logout">
        <p>  <a class='link-button' href="{{ path('clients') }}">back to list</a> </p>
    </div>
    <button class="btn text-center " id='homebutton'>MyEnesi </button>
    <div class="my-card m-3 p-2">
        <h2 class="text-center">{{ client.name }}</h2>
        <p > name </p>
            <h5 class="card-title">{{ client.name }}</h5> 
        <p> Place </p>
            <h5 class="card-title">{{ client.place }}</h5> 
        <p> Status </p>
            <h5 class="card-title">{{ client.status }}</h5> 
        <a style="height: 100%"  href="{{ path('client_edit', {'id': client.id}) }}" class="tablebutton"> edit</a>
        {{ include('client/_delete_form.html.twig') }}
    </div>
{% endblock %}
