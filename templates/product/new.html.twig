{% extends 'base.html.twig' %}

{% block title %}New Product{% endblock %}

{% block body %}
    <div class="logout">
        <p>  <a class='link-button' href="{{ path('products') }}">back to list</a> </p>
    </div>
    <div class="my-card m-3 p-2">
        <h2>Create new Product</h2>
        {{ include('product/_form.html.twig') }}   
    </div>
{% endblock %}
